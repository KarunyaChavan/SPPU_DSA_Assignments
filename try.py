#For Chaining
                    # j = 0 
                    # while(j<self.M):#Traversing the whole table
                    #     if((self._table[j].key)%self.M==key%self.M and self._table[j].chain==-1): #Checking for synonyms and empty chain fields
                    #         self._table[j].chain = self.index
                    #         break
                    #     j+=1